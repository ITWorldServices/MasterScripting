A simple script to backup the proxaddress field in AD objects before making changes to fix issues, possibly identified by IDFix

Steps to deploy
Copy the script to the Active Directory server into the temp directory
Open a Powershell window and change to the C:\temp folder
Run ".\proxyaddressbackup.ps1"

A file will be generated and placed into the C:\temp folder.
